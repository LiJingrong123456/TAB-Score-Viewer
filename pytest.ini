# pytest 配置文件
# 说明: 将项目根加入 sys.path, 使 `import models` 等顶层模块生效 (与 conftest.py 重复保险)

[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# 输出选项
addopts = -ra --strict-markers --tb=short

# 标记
markers =
    slow: 慢速测试 (真实 GTP 解析等)
