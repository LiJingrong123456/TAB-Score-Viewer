# 功能更新记录

## v1.9.0 (2026-06-12) - 多语言支持(i18n)

### 新功能
- **国际化系统(I18n)**: 完整的多语言支持框架
  - **单例模式**: I18n 类全局唯一实例，统一管理翻译状态
  - **JSON翻译文件**: locales/ 目录下按语言存放翻译文件（zh_CN.json / en_US.json）
  - **占位符支持**: 使用 Python str.format() 语法，如 `"位置: {pct}%"`
  - **语言回退机制**: 缺失翻译自动回退到中文(zh_CN)，确保UI不会出现空白
  - **信号通知**: language_changed 信号，连接后可在语言变更时刷新UI
  - **配置持久化**: 语言选择保存到 settings.json 的 `language` 字段

- **语言切换UI**: 设置窗口新增语言选择下拉框
  - 支持中文(简体)和英文两种语言
  - 切换后立即保存配置，新打开的窗口使用新语言
  - 提示用户部分静态文本需重启应用完全生效

- **全面国际化覆盖**:
  - 主窗口(DisplayWindow): 工具栏/控制面板/状态信息/消息提示
  - 设置窗口(SettingsWindow): 文件夹选择/搜索/右键菜单/错误提示
  - 对话框: 标注创建/编辑/管理器、导出对话框、速度曲线编辑器
  - 底部进度栏、音频模式按钮、A-B循环提示等

### 技术原理
- **翻译键命名规范**: 模块.子模块.具体项 (如 control_panel.play_btn)
- **调用方式**: `I18n.t("key", **kwargs)` 获取翻译文本
- **语言检测**: 启动时从 settings.json 读取已保存的语言设置
- **扩展性**: 新增语言只需在 locales/ 目录添加对应的 JSON 文件

### 修改文件
- **重构** `guitar_tab_viewer.py` — 新增I18n类 + 所有硬编码字符串替换为t()调用 + 语言切换UI
- **新增** `locales/zh_CN.json` — 简体中文翻译文件（完整覆盖所有UI文本）
- **新增** `locales/en_US.json` — 英文翻译文件（完整覆盖所有UI文本）

---

## v1.8.2 (2026-06-12) - 库名更名 + 许可证更新

### 重构内容
- **库名更改**: `gtp_engine` → `ApolloTab`
  - 目录重命名: gtp_engine/ → ApolloTab/
  - PyPI包名: gtp-engine → ApolloTab
  - 所有导入语句: `from gtp_engine import ...` → `from ApolloTab import ...`
  - 配置文件更新: pyproject.toml, MANIFEST.in
  - 文档更新: README.md, __init__.py, 示例代码

- **许可证更改**: MIT → Mozilla Public License 2.0 (MPL-2.0)
  - 更新 pyproject.toml 中的 license 字段和 classifiers
  - 更新 README.md 中的 License 徽章
  - 更新 LICENSE 文件为 MPL 2.0 标准文本

- **Bug修复**: guitar_tab_viewer.py 多处库化残留问题
  - **#1 语法错误** (第2605行): `_update_playhead()` 方法中有重复的 `else:` 语句 → 删除重复分支
  - **#2 ImportError** (player.py): `RenderConfig` 导入路径错误，应从 `.utils` 导入而非 `.renderer`
  - **#3 AttributeError** (第1378行): mousePressEvent 中残留旧属性 `_synth_engine` 等 → 替换为 `gtp_player` 调用
  - **#4 属性名错误**: `audio_initialized` 不存在 → 改为 `is_audio_ready`
  - **#5 播放条/小节高亮不显示**: `_on_content_loaded()` 缺少 `_build_playhead_timeline()` 调用 → 补充调用
  - 验证: Python 语法检查通过 (py_compile)

### 技术原理
- **MPL 2.0 特点**: 
  - 允许商业使用和修改（比GPL宽松）
  - 要求修改后的文件必须开源（比MIT严格）
  - 专利授权保护（MIT没有）
  - 兼容 LGPL/GPL（可作为更大项目的一部分）

### 修改文件
- **重命名** `gtp_engine/` → `ApolloTab/` — 整个库目录
- **更新** `pyproject.toml` — 包名、URLs、包数据配置
- **更新** `MANIFEST.in` — 包含路径
- **更新** `guitar_tab_viewer.py` — 导入语句 + 修复语法错误
- **更新** `examples/*.py` — 所有示例的导入语句和文档
- **更新** `ApolloTab/README.md` — 完整文档（库名、安装命令、代码示例）
- **更新** `ApolloTab/__init__.py` — 文档字符串和导入说明
- **更新** `LICENSE` (根目录+库目录) — MPL 2.0 许可证文本

---

## v1.8.1 (2026-06-12) - 主程序GTP代码库化完成

### 重构内容
- **完全解耦**: 主程序 `guitar_tab_viewer.py` 中所有GTP相关代码已迁移至 `gtp_engine` 库
  - 删除内联的 `_synth_engine`, `_midi_converter`, `_gtp_song`, `_audio_events` 等属性
  - 删除内联的 `_build_playhead_timeline()`, `_update_playhead()`, `_time_to_scroll_pos()`, `_scroll_pos_to_time()` 方法实现
  - 统一使用 `GTPPlayer` 高级封装类（整合解析/渲染/音频/时间线功能）

- **接口统一**: 所有GTP操作通过 `self.gtp_player` 单一入口访问
  - 文件加载: `player.load()` → 自动解析+渲染+准备音频数据
  - 音轨管理: `player.current_track`, `player.get_all_tracks_info()`, `player.render_track()`
  - 播放控制: `player.play()`, `player.stop()`, `player.seek()`, `player.shutdown()`
  - 时间线: `player.build_timeline()`, `player.time_to_scroll_pos()`, `player.scroll_pos_to_time()`
  - 音频模式: `player.init_audio()`, `player.set_audio_mode()`

- **代码清理**: 移除约200行冗余代码，主程序更简洁易维护
  - 播放控制方法（start/closeEvent/stop_playback）改用gtp_player调用
  - 进度条拖动同步简化为单次`player.seek()`调用
  - 标注系统适配：通过`player.current_track`和`player.file_path`获取状态

### 技术原理
- **封装模式**: GTPPlayer作为Facade（外观模式），隐藏内部复杂实现细节
- **延迟初始化**: GTPPlayer在加载GTP文件时创建，非GTP文件时为None（零开销）
- **状态同步**: gtp_player内部维护完整的音频/时间线状态，主程序只做UI展示

### 修改文件
- **重构** `guitar_tab_viewer.py` — 删除所有内联GTP代码，改为调用gtp_player库
- **新增** `gtp_engine/player.py` — GTPPlayer高级播放器封装类（Phase 4已创建）
- **更新** `gtp_engine/__init__.py` — 导出GTPPlayer和相关工厂函数

---

## v1.8.0 (2026-06-12) - Phase 4: gtp_engine 库化（PyPI发布）

### 新功能
- **PyPI包发布配置**: 新增 `pyproject.toml` 配置文件，支持标准Python包构建和发布
  - 使用现代 setuptools + wheel 构建系统（兼容 Python 3.8+）
  - 完整的元数据定义：名称/版本/描述/作者/分类器/依赖
  - 支持可选依赖组：`full`（完整功能）, `dev`（开发环境）
  - 包含 black/isort/mypy 代码质量工具配置

- **独立包结构优化**: gtp_engine 完全解耦，可作为独立库安装使用
  - 零主程序依赖：无任何对 guitar_tab_viewer.py 的引用
  - 添加 PEP 561 `py.typed` 类型提示标记文件
  - 完善的子模块 `__init__.py` 导出定义
  - 支持 `pip install .` 或 `pip install -e .` 开发模式安装

- **完整文档体系**:
  - `gtp_engine/README.md`: PyPI级README，含快速开始/API参考/配置说明
  - `examples/basic_parse.py`: 基础解析示例（提取歌曲信息/音轨列表）
  - `examples/render_tab.py`: 渲染示例（自定义参数/批量转换）
  - `examples/audio_playback.py`: 音频播放示例（交互控制/进度显示）

- **许可证与分发**:
  - MIT License 许可证文件 (`gtp_engine/LICENSE`)
  - MANIFEST.in 打包配置（包含README/LICENSE/示例代码）

### 技术原理
- **pyproject.toml**: PEP 517/518 标准，替代传统 setup.py/setup.cfg
- **模块解耦**: gtp_engine 仅依赖 pyguitarpro/PyQt5/pyfluidsynth，无项目内部耦合
- **类型支持**: py.typed 文件使 mypy 等工具可检查库的类型注解

### 使用方式
```bash
# 安装（从PyPI，发布后）
pip install gtp-engine

# 或从源码安装
cd "gtp_engine所在目录"
pip install -e .

# 使用示例
from gtp_engine import parse_gtp, render_gtp, SynthEngine
song = parse_gtp("my_song.gp5")
pages = render_gtp("my_song.gp5", track_index=0)
```

### 修改文件
- **新增** `pyproject.toml` — PyPI包构建配置
- **新增** `gtp_engine/README.md` — 库文档（快速开始/API/配置）
- **新增** `gtp_engine/LICENSE` — MIT许可证
- **新增** `gtp_engine/py.typed` — PEP 561类型提示标记
- **新增** `MANIFEST.in` — 打包文件清单
- **新增** `examples/basic_parse.py` — 解析示例代码
- **新增** `examples/render_tab.py` — 渲染示例代码
- **新增** `examples/audio_playback.py` — 音频播放示例代码
- **更新** `gtp_engine/__init__.py` — 完善导出列表和文档字符串

---

## v1.7.1 (2026-06-11) - 性能优化专项

### 性能优化
- **total_h缓存(高优先级)**: `_hit_annotation/_draw_annotations/mouseMoveEvent/mouseDoubleClickEvent`
  4处重复计算 `sum(img.height()*ratio+5)` → 统一缓存到 `_cached_total_h`，images/width变化时失效
  - 消除每次鼠标移动/绘制时的O(n)浮点运算重复
  - 新增 `DisplayWidget._get_total_h()` 方法统一获取

- **bisect关键列表预提取(高优先级)**: `_update_playhead/_time_to_scroll_pos/_scroll_pos_to_time` 
  3个方法每帧都创建新列表 `[e['time_ms'] for e in timeline]` 做二分查找
  → 在 `_build_playhead_timeline` 时一次性构建 `._timeline_times` 和 `._timeline_scroll_ys` 排序列表
  → 后续直接复用，消除每帧30fps×3方法=90次/秒的O(n)内存分配和GC压力
  - 同时将 `import bisect` 从函数内移到文件顶层（优化5）

- **速度曲线points排序缓存(中优先级)**: `_get_curve_speed()` 每帧执行 `sorted(points)`
  → 通过 `id()` 检测points列表引用是否变化，仅在变化时重新排序
  → 正常播放期间0次排序开销（曲线编辑后首次调用排序1次）

- **deepcopy替换为copy(中优先级)**: `tab_renderer.py` 首页首行15px偏移使用 `copy.deepcopy(system)`
  → 改用 `copy.copy()` 浅拷贝（仅修改4个int属性，无需深拷贝嵌套measures列表）
  → 渲染首页时减少一次完整的系统布局树深拷贝

### 技术原理
- **缓存失效策略**: dirty flag + 宽度比对（与图片缩放缓存同一模式）
- **id()脏检测**: Python对象identity不变表示列表未被重新赋值，O(1)检测
- **浅拷贝安全性**: SystemLayout的measures列表在偏移场景下只读不修改，共享引用安全

### 修改文件
- `guitar_tab_viewer.py` — total_h缓存 + bisect预提取列表 + 速度曲线排序缓存 + import bisect顶层化
- `gtp_engine/renderer/tab_renderer.py` — deepcopy→copy + 移除无用import copy

---

## v1.7.0 (2026-06-09) - 点击位置非线性时间映射修复

### Bug修复
- **点击谱面位置误差较大(提前)**: mousePressEvent用线性比例`pos/total*time`反推音频时间
  - 根因: `_build_playhead_timeline`建立的是**非线性映射**(密集区每像素对应少时间，稀疏区多时间)
  - 线性比例在密集区**高估时间**→ 音频跳到点击位置后面 → 用户感觉"提前了"
  - 修复: 新增`_scroll_pos_to_time()`方法，在timeline中对scroll_y做二分查找+插值(与_time_to_scroll_pos完全对称的逆运算)
  - GTP模式用精确反向查找，非GTP模式降级为线性比例

### 技术原理
- 正向映射: `time_ms → _time_to_scroll_pos() → 二分查找time → 插值scroll_y → 居中 → current_position`
- 反向映射: `current_position → _scroll_pos_to_time() → 还原raw_scroll_y → 二分查找scroll_y → 插值time_ms → seek`
- 两者完全对称，保证点击位置与播放位置精确一致

### 修改文件
- `guitar_tab_viewer.py` — 新增_scroll_pos_to_time(); mousePressEvent使用反向查找替代线性比例

---

## v1.6.9 (2026-06-09) - seek静音 + 点击位置修正 + ×按钮区域扩展

### Bug修复
- **播放中点击出现双重声音**: seek()停旧线程但未静音已发声音符，新旧音频叠加
  - 根因: seek()只设置_stop_flag+join线程，旧note_on事件已发到FluidSynth仍在发声
  - 修复: 新增`silence_all_notes()`方法(全通道noteoff+pitch_bend复位)，在seek()的stop_flag之前调用
- **点击位置误差较大(提前)**: mousePressEvent中错误地乘以比例系数`total_scroll_distance/total_h`
  - 根因: `current_position`和`total_scroll_distance`都是像素单位(paintEvent直接用-current_position做偏移)，不需要比例转换。该比例<1导致结果偏小
  - 修复: 去掉比例系数，改为 `new_position = click_abs_y - height/2`(点击处居中显示)
- **标注×删除按钮移到时消失**: ×按钮画在bg_rect右侧外部，鼠标移过去离开命中区域→悬停清除→按钮消失
  - 修复: `_hit_annotation()`右边界扩展28px(含×按钮宽度18px+间距+余量)


---

## v1.6.8 (2026-06-09) - synth_engine seek偏移修复 + 推弦pre-note-on复位

### Bug修复
- **点击谱面播放条回到开头(根因修复)**: synth_engine.current_time_ms属性计算公式不包含seek偏移
  - 根因: `current_time_ms = (perf_counter - _start_time) * 1000 - paused_duration`，完全忽略seek设置的_current_time_ms
  - 现象: seek到5000ms后play()，33ms后current_time_ms返回≈33ms而非5033ms → _time_to_scroll_pos→≈0
  - 修复: 新增`_initial_time_offset`实例变量，在play()/seek()/_play_loop()中同步维护
  - 公式改为: `current_time_ms = elapsed - paused + _initial_time_offset`
  - stop()中重置所有通道pitch_bend=8192 + 重置offset=0
- **推弦后后续音符音高变高(根本性修复)**: 改用"防御性复位"策略
  - 原方案(失败): note_off后发送pitch_bend=8192复位 — 有时序风险，可能被跳过或延迟
  - 新方案: **每个note_on前检查并复位pitch_bend** — 无论前一个音符的弯音状态如何，当前音符都以干净状态开始
  - 逻辑: 无推弦/推弦起点为0的音符 → note_on前发8192复位; 有推弦且从原位开始 → 不需要预先复位
  - 效果: 即使之前的复位事件被跳过(seek等)，下一个note_on也会自动修正音高


## 待规划功能
- [x] ~~GTP解析渲染~~ ✅ v1.1.0 | ~~音频播放~~ ✅ v1.3.0 | ~~技巧符号~~ ✅ v1.2.x | ~~多轨~~ ✅ v1.1.1 | ~~库化~~ ✅ v1.8.x | ~~多语言~~ ✅ v1.9.0
- [ ] 缩放 | 标注导入导出(已有基础) | 最近文件 | 全屏模式
