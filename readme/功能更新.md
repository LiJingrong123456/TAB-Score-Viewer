# 功能更新记录

## v2.0.6 (2026-06-14) - GTP音轨音量控制(专业DAW混音器风格)

### 增量修复

| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 1 | **A/B循环到达B点后不seek回A点** | UI层`_tick()`在`effective_time >= total_dur`时提前调用`stop_playback()`，导致库层来不及触发循环重启 | region循环模式下跳过UI层播放结束检测，交给库层管理循环生命周期 |
| 2 | **点击谱面跳转到小节中间位置** | 旧方案使用精确像素定位，可能跳到小节内部任意位置，与A/B循环的小节原子单位不一致 | 重构为以"小节"为单位定位: 点击任意位置自动对齐到所在小节的起始拍 |
| 3 | **A/B循环激活时点击区间外小节会跳出循环** | 点击跳转未检查A/B循环范围，任何位置都可跳转 | 循环激活时判断目标小节是否在[loop_start, loop_end]区间内，区间外则忽略跳转 |
| 4 | **每行(System)小节索引重复导致跨行定位错误** | `build_timeline()`中`meas_idx`通过`enumerate(system.measures)`获取，每个系统(行)内从0重新计数，导致不同系统的同名小节(如都是小节0)被合并 → 循环设置/点击跳转选错小节 | 引入全局唯一ID `global_meas_idx`(递增计数器)，保留原`meas_idx`用于UI显示；所有内部匹配使用`global_meas_idx`确保跨系统/页唯一性 |
| 5 | **点击空白小节(无音符)误差大** | 空白小节在`build_timeline()`中被`continue`跳过，不生成timeline条目 → 点击时二分查找落到相邻有音符的小节 | 为空白小节生成单个占位条目(`beat_idx=-1`)，记录其起始scroll_y和time_ms，使点击可正确定位 |

### 新功能

- **小节单位点击跳转(与A/B循环联动)**:
  - GTP模式: 点击谱面任意位置 → 自动对齐到所在小节的起始拍并开始播放
  - A/B循环联动:
    - 循环激活 + 点击区间外小节 → 忽略跳转，维持循环播放
    - 循环激活 + 点击区间内小节 → 跳转到该小节开头并继续循环
    - 循环未激活 → 正常跳转到点击的小节开头
  - 非GTP模式(图片/PDF): 降级为原始像素级跳转(无时间线数据)
  - 边界容差±50ms: 避免边界值因时间精度被误判为"区间外"

- **GTPPlayer新增API**:
  - `find_measure_at_scroll_pos(scroll_y)`: 根据滚动Y位置查找所在小节信息(返回meas_idx/start_time_ms/start_scroll_y)
  - `loop_time_range`属性: 获取当前A/B循环的时间范围(毫秒元组)

- **GTP音轨音量调节滑块**:

- **TrackVolumeSlider组件**:
  - 自定义垂直音量滑块QWidget(继承QObject支持pyqtSignal)
  - dB值与像素坐标双向转换(_db_to_y / _y_to_db)
  - 渐变填充轨道 + 3D立体手柄效果
  - 动态主题适配(深色/浅色模式自动切换配色)
  - 信号机制: valueChanged.emit(db_value) 实时通知音量变化

### 技术实现

- **UI布局**: QGroupBox → QHBoxLayout → QScrollArea → QWidget(容器) → 多个TrackVolumeSlider
- **动态创建**: _setup_volume_controls() 根据GTP文件音轨数量动态生成滑块
- **数据存储**: _track_volumes: Dict[int, float] 存储各轨音量, _master_volume存储总音量
- **音频引擎集成**: 尝试调用 GTPPlayer.set_track_volume() / set_master_volume() (如果底层支持)
- **可见性控制**: 仅GTP文件显示，非GTP文件/关闭时自动隐藏

### UI参数(可调整)

| 参数 | 默认值 | 说明 | 调整效果 |
|------|--------|------|----------|
| SLIDER_WIDTH | 22px | 滑块轨道宽度 | 越宽越粗 |
| HANDLE_HEIGHT | 28px | 手柄高度 | 越高越容易抓取 |
| WIDGET_WIDTH | 56px | 组件总宽度 | 影响占用空间 |
| WIDGET_HEIGHT | 180px | 组件总高度 | 影响刻度精度 |
| SCALE_WIDTH | 20px | 刻度区宽度 | 刻度文字显示空间 |
| volume_scroll.setMaximumHeight | 200px | 滚动区域最大高度 | 防止撑开面板 |

### 修改文件

| 文件 | 改动 |
| --- | --- |
| `ApolloTab/player.py` | 新增`find_measure_at_scroll_pos()`方法(坐标→小节定位); 新增`loop_time_range`属性(获取循环时间范围) |
| `TAB Score Viewer.py` | 重构`DisplayWidget.mousePressEvent()`: 点击跳转改为小节单位定位; 新增A/B循环区间判断(区间外忽略跳转); `_tick()`修复region循环提前停止问题 |
| `locales/zh_CN.json` | 新增`control_panel.volume_group`(1个翻译键) |
| `locales/en_US.json` | 新增对应的英文翻译键(1个) |

### 10条Case预期结果

| Case | 场景 | 预期结果 |
| --- | --- | --- |
| 1 | GTP模式+无循环: 点击第5小节中间位置 | 跳转到第5小节起始拍位置并开始播放 |
| 2 | GTP模式+A/B循环[2-8]+点击第3小节(区间内) | 跳转到第3小节开头，继续A/B循环播放 |
| 3 | GTP模式+A/B循环[2-8]+点击第1小节(区间外) | 忽略跳转，控制台输出"在循环区间之外"，维持当前循环 |
| 4 | GTP模式+A/B循环[2-8]+点击第10小节(区间外) | 同Case3，不执行跳转 |
| 5 | GTP模式+A/B循环激活+点击边界小节(±50ms容差内) | 允许跳转(边界容差防止误判) |
| 6 | 图片/PDF文件(非GTP)+点击谱面 | 降级为原始像素级跳转(无时间线数据) |
| 7 | 点击谱面时未在播放状态 | 跳转到目标小节并自动启动播放 |
| 8 | 点击谱面时正在播放中 | 热seek到目标小节，不中断播放流 |
| 9 | A/B循环未激活(region模式但未设点) | 正常跳转到点击的小节开头(无约束) |
| 10 | all循环模式(全曲循环)+点击任意小节 | 不受循环限制，正常跳转(all模式无区间约束) |

---

## v2.0.5 (2026-06-14) - 打印曲谱功能(含标注，支持分轨/分页/预览)

### 新功能

- **打印曲谱到打印机**:
  - 工具栏新增"打印"按钮(下拉菜单: 直接打印 / 打印预览)
  - 支持选择轨道(GTP多轨时: 当前轨/全部轨/指定轨道)
  - 支持选择页码范围(全部页面/指定范围)
  - 弹出系统打印对话框(QPrintDialog)让用户选择打印机
  - 自动分页: 内容超过一页A4时自动分多页打印
  - **包含所有标注**: 完整渲染标注(颜色/字体/大小/背景色)，排序与导出功能完全一致
  - **GTP文件强制浅色主题**: 与导出一致，确保打印输出清晰可读

- **打印预览功能**:
  - 下拉菜单选择"打印预览..."后显示QPrintPreviewDialog窗口
  - 预览窗口支持缩放(50%~400%)、翻页、多页视图
  - 可在预览中调整打印机参数(纸张方向/边距等)
  - 点击预览窗口的"打印"按钮直接发送到打印机
  - 最小窗口尺寸800×600px确保预览效果良好

### 技术实现

- **复用导出逻辑**: 打印渲染核心`_render_to_printer()`完全复用A4导出的缩放+预渲染防溢出方案
  - 按打印机可打印区域宽度等比缩放图片
  - 使用"预渲染+合成"方案(page_buffer → QPrinter)杜绝内容溢出
  - 标注绘制复用`_draw_annotations_on_canvas()`方法
  - 跳过近空白尾页检测(阈值5%)
- **架构重构**: `_print_score(use_preview=False)` 统一入口
  - `_execute_print()`: 直接打印流程(QPrintDialog → _render_to_printer)
  - `_show_print_preview()`: 预览流程(QPrintPreviewDialog + paintRequested回调)
- **对话框**: 新增`PrintDialog`类(基于ExportDialog简化，移除格式选择部分)
- **工具栏按钮**: 使用QToolButton.MenuButtonPopup模式实现下拉菜单
- **图标**: 新增`icons/printer.svg`(Lucide风格打印机图标)

### 修改文件

| 文件 | 改动 |
| --- | --- |
| `TAB Score Viewer.py` | 重构`_print_score()`; 新增`_execute_print()`, `_show_print_preview()`; 工具栏改为下拉菜单 |
| `locales/zh_CN.json` | 新增`toolbar.print_direct`, `toolbar.print_preview`, `print_dialog.preview_title`(3个新键) |
| `locales/en_US.json` | 新增对应的英文翻译键(3个) |
| `icons/printer.svg` | 新增打印机SVG图标 |

### 10条Case预期结果

| Case | 场景 | 预期结果 |
| --- | --- | --- |
| 1 | 单张图片谱面+无标注 | 正常打印1页A4纸；预览显示1页完整内容 |
| 2 | 多页PDF谱面(10页)+3个标注 | 自动分10页打印；预览可翻页查看每页标注位置 |
| 3 | GTP单轨+5个标注 | 强制浅色主题渲染后打印；所有标注正确显示 |
| 4 | GTP多轨(4轨)+全部轨道模式 | 按1→2→3→4顺序依次打印各轨；预览显示所有页面 |
| 5 | GTP多轨+指定轨道(1,3轨) | 仅打印轨道1和3；预览仅显示这两轨的内容 |
| 6 | 选择页码范围(2-5页) | 仅打印第2~5页；预览只显示这4页 |
| 7 | 标注跨页(位置在第1页底部和第2页顶部) | 两页分别显示对应部分的标注；预览可清晰看到跨页效果 |
| 8 | 点击"直接打印..." | 弹出PrintDialog → QPrintDialog → 直接发送到打印机 |
| 9 | 点击"打印预览..." | 弹出PrintDialog → QPrintPreviewDialog(可缩放翻页) → 点击打印按钮发送 |
| 10 | 无内容时点击打印按钮 | 提示"无法打印：当前没有可打印的谱面内容" |

---

## v2.0.4 (2026-06-14) - A/B循环架构重构(下沉到库层，基于小节原子单位)

### 架构变更

- **A/B循环逻辑从UI层完全移除，下沉到库层(SynthEngine音频线程内部)**:
  - 用户核心思路: "把每个小节都作为一个模块，A在哪个小节就从那个小节开始播，B在哪个小节就在那个小节末尾结束"
  - **旧方案的问题**(v2.0.3及之前): UI层用帧计数器/冷却/模拟时钟/安全边界等复杂机制试图解决竞态→每修一个bug引出新的→无限循环
  - **新方案的原理**:
    1. `SynthEngine._play_loop()` 内置循环: 播放完所有事件后检查`_loop_enabled`标志 → 若启用则静音→重置时间基准→重新遍历事件列表（早于loop\_start的事件被time检查自动跳过）
    2. `GTPPlayer` 提供基于小节的API: `set_loop_region_by_position(pct, pct)` 将百分比→转换为小节索引→对齐到小节边界(起始小节第一拍/结束小节最后一拍)
    3. UI层 `_tick()` 恢复为干净的"读audio\_time\_ms→算position"，零循环相关代码
  - **彻底消除的问题**: 竞态条件、冷却期、模拟时钟、安全边界、停顿、跳动、卡死 — 全部不存在了

### 修改文件

| 文件                                | 改动                                                                                                                                                                                                              |
| --------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ApolloTab/audio/synth_engine.py` | 新增 `_loop_enabled/_loop_start_ms/_loop_end_ms` 属性; 新增 `set_loop_region()/clear_loop_region()` 方法; `_play_loop()` 从for改为while循环，支持内部循环重启                                                                         |
| `ApolloTab/player.py`             | 新增 `set_loop_region()`, `set_loop_region_by_measure()`, `set_loop_region_by_position()`, `clear_loop_region()`, `_find_measure_at_time()`, `is_loop_enabled` 属性                                                 |
| 主文件                               | **删除**: `_loop_seek_cooldown/_target_ms/_sim_time` 变量, `_execute_loop_seek()` 方法; **简化** `_tick()` 时间驱动段(\~110行复杂逻辑→\~25行干净代码); **重写** `_on_loop_mode_changed()`/`_set_ab_point()`/`_clear_ab_points()` 使用库层API |

### 10条Case预期结果

| Case | 场景                     | 预期结果                  |
| ---- | ---------------------- | --------------------- |
| 1    | 第1小节短循环(A-B跨1-2个小节)    | 平滑A→B→A循环，无卡顿无跳动      |
| 2    | 极短循环(单个小节内)            | 以完整小节为单位循环，稳定可靠       |
| 3    | 正常循环 A=20%, B=60%      | 行为不变，正常循环             |
| 4    | A=B（同一个小节）             | 循环该小节(从开头到结尾)         |
| 5    | 播放中切换到A-B模式（当前位置>B）    | 库层立即设置循环范围，下次循环周期自动生效 |
| 6    | 播放中切换到A-B模式（当前位置在AB之间） | 继续播放，到达B后由库层自动循环回A    |
| 7    | 先设B后设A（A>B自动交换）        | 交换后正常工作               |
| 8    | 循环中拖动进度条到AB外           | 拖动后从新位置继续/停止          |
| 9    | GTP有音频 vs 无音频(off模式)   | off模式走线性路径不受影响        |
| 10   | 速度曲线开启状态下的短循环          | 曲线乘数不影响循环逻辑           |

### 增量修复 (v2.0.4 迭代调试中修复的6个问题)

| # | 问题                                      | 根因                                                     | 修复                                                  |
| - | --------------------------------------- | ------------------------------------------------------ | --------------------------------------------------- |
| 1 | **设A/B按钮pct全是0%** → 小节\[0-0]            | `_set_ab_point()`用UI滚动位置`current_position`(未播放时=0)算百分比 | 改为优先用GTP音频时间 `audio_time_ms / total_duration_ms`    |
| 2 | **切换到region模式后不跳回A点**                   | `set_loop_region()`只设参数不seek，音频继续在当前位置播                | `_on_loop_mode_changed()`增加立即`seek(target_ms)`到A点   |
| 3 | **短循环先播完整首谱才回来**                        | 库层循环只在所有事件播完后才触发(while结束条件)                            | 新增事件级检查: `target_time_ms >= loop_end_ms` 时立即静音+重启循环 |
| 4 | **B点设在谱末尾附近返回meas\_idx=0**              | 时间线哨兵点(末尾虚拟点)的meas\_idx硬编码为0                           | 哨兵点继承最后1个真实条目的索引值                                   |
| 5 | **进度条Ctrl/Shift设AB后不同步库层**              | `_on_region_selected()`只更新了UI没调库层API                   | region模式下立即同步调用`set_loop_region_by_position()`      |
| 6 | **seek()竞态: 返回后读current\_time\_ms异常偏大** | 音频线程还没来得及设置`_start_time`                               | `seek()`Step4.5在锁内预置`_start_time`                   |

***

## v2.0.2 (2026-06-14) - 图标修复 + UI优化 + 标注快捷键增强

### Bug修复

- **A-B区域循环"播不了"问题**: 切换循环模式时若position>=B点则死循环 → 修复: 立即seek+帧保护(v2.0.3已重构为此方案的改进版)
- **打包后应用图标缺失**: PyInstaller onedir模式图标在\_internal/子目录 → 修复: 双重路径查找
- **QContextMenuEvent未导入**: 新增右键菜单功能时漏导 → 修复: 添加导入
- **双击标注直接删除**: 删除判断过于宽松(整个标注区域) → 修复: 仅×按钮+200ms延迟
- **翻译键缺失29个**: annotation\_create/manager/edit相关键 → 补充到zh\_CN.json/en\_US.json

### UI优化

- 音频按钮图标增大到24x24, ToolButtonTextBesideIcon样式
- 移除状态信息栏(进度条已可显示位置)

### 功能变更

- 移除双击谱面创建标注(统一Ctrl+K和右键菜单)

### 新功能

- Ctrl+K快捷键创建标注
- 右键谱面"添加标注"菜单

***

## v2.0.1 (2026-06-14) - SVG图标系统 + 翻译修复 + 文档优化

### UI优化

- SVG图标系统(Lucide风格, 13个图标), 替移所有emoji
- ModernButton支持icon\_name参数自动加载

### Bug修复

- 播放按钮样式突变 → ModernButton.set\_color()语义化调用
- GTP点击前几行无法跳转 → 完全去掉offset=0精确定位
- 翻译键缺失30个(settings\_window.\*) → 补充

### 打包优化

- SoundFont路径修正为\_internal/soundfont/(PyInstaller onedir)

## 待规划功能

- [x] ~~GTP解析渲染~~ ✅ v1.1.0 | ~~音频播放~~ ✅ v1.3.0 | ~~技巧符号~~ ✅ v1.2.x | ~~多轨~~ ✅ v1.1.1 | ~~库化~~ ✅ v1.8.x | ~~多语言~~ ✅ v1.9.0 | ~~图标+打包~~ ✅ v1.9.1 | ~~主题系统~~ ✅ v2.0.0
- [ ] 标注导入导出(已有基础) | 最近文件 | 全屏模式

