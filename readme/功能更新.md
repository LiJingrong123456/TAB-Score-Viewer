# 功能更新记录

## v1.6.0 (2026-06-08) - 导出系统重构 + 播放平滑优化 + 时间驱动滚动

### Bug修复
- **导出PNG/PDF为0字节**: 使用QImage替代QPixmap作为画布缓冲区，确保`painter.end()`在`save()`之前调用，修复数据未flush导致空文件
- **播放条卡顿不连贯**: 根因是定时器间隔=base_speed(350-700ms)，每秒仅更新1-3次
  - 修复: 改用固定30fps定时器(33ms)，通过调整每帧scroll_step控制播放速度
  - `_calculate_scroll_step()`: 新公式使用固定FRAME_INTERVAL_MS=33替代base_speed
  - `start_playback()`: `timer.start(33)` 替代 `timer.start(base_speed)`
  - 效果: 播放条和谱面滚动从"跳一下动一下"变为平滑连续移动
- **播放条速度不随音符时值变化**: 根因是滚动位置线性恒速递增，与音乐时间脱节
  - 修复: 新增`_time_to_scroll_pos()`方法，用音频时间→时间线索引二分查找+线性插值获取滚动Y位置
  - `_build_playhead_timeline()`: 每个拍新增`scroll_y`字段(累计页面高度+系统Y偏移)
  - `_tick()`: 有音频引擎时切换为**时间驱动模式**(`synth_engine.current_time_ms` → scroll_pos)，无音频时降级为线性模式
  - 效果: 音符密集区(16/32分音符)自动加快滚动，稀疏区(全/二分音符)自动减慢，与实际节奏同步
- **ExportDialog类缩进错误**: 插入ExportDialog后后续方法被挤出DisplayWindow类体变成模块级函数
  - 修复: 将`_open_speed_curve_editor`等5个方法移回DisplayWindow类内部正确位置
- **QRadioButton未导入**: ExportDialog使用了QRadioButton但未在QtWidgets导入列表中

### 新增功能/重构
- **导出对话框(ExportDialog)**: 自定义对话框替代原生文件选择器，支持:
  - 格式选择: PNG(每页一张图片) / PDF(单文件多页)
  - 分轨导出(GTP多轨): 仅当前轨 / 全部轨道(按顺序拼接，文件名带tN后缀) / 指定轨道(checkbox多选)
  - 分页导出: 全部页面 / 指定范围(起始页~结束页)
- **分轨标注渲染**: 导出时自动加载对应轨道的独立标注文件(`.tN.anno.json`)
- **PNG每页一图**: 多页内容自动生成 `xxx_p1.png`, `xxx_p2.png`, ...
- **PDF单文件**: 所有页面合并在一个PDF中，自动分页

### 修改文件
- `guitar_tab_viewer.py` — 新增 ExportDialog 类; 重写 _export_to_a4 / _collect_export_data / _get_page_range_images / _load_track_annotations; 新增 _render_to_a4_png_v2(QImage+painter.end修复) / _render_to_a4_pdf_v2(setFileName修复); start_playback/timer.start改为33ms固定帧率; _calculate_scroll_step公式重写; 新增 _time_to_scroll_pos(时间→滚动映射) + _build_playhead_timeline(scroll_y字段); _tick()双模式(时间驱动/线性降级); 导入增加QRadioButton

---

## v1.5.1 (2026-06-07) - 标注分轨(按音轨独立存储)

### Bug修复
- **标注没有分轨**: GTP多轨文件切换音轨时，标注现在按音轨独立存储和加载
  - 每个音轨有独立的标注文件: `song.gp4.t0.anno.json`, `song.gp4.t1.anno.json`, ...
  - 非GTP/单轨文件不变: `filename.anno.json` (与之前一致)
  - 切换音轨时自动保存当前轨标注 → 加载目标轨标注 → 清空撤销栈
  - 内存缓存: `_annotations_by_track` 字典避免重复文件IO

### 修改文件
- `guitar_tab_viewer.py` — 新增 `_annotations_by_track` 分轨字典 + `_switch_track_annotations()` 切轨方法; `_get_annotation_file_path()` 支持GTP多轨命名; `_on_gtp_track_changed()` 调用分轨切换

---

## v1.5.0 (2026-06-07) - 标注拖动 + 速度范围调整 + 播放性能优化

### 新增功能
- **标注拖动移动**: 左键按住标注可拖动到谱面任意位置
  - 悬停标注时显示手型光标(OpenHandCursor)，拖动中显示抓手(ClosedHandCursor)
  - 拖动过程中实时预览位置，松开鼠标后保存快照+写入文件
  - 位置无变化(误触)时不入撤销栈，避免无意义记录
  - 拖动不干扰双击编辑(单击拖动 vs 双击编辑自动区分)

### 调整
- **速度范围调整为350-700ms**: 原范围1-500ms太宽且默认值过小导致播放过快
  - 新默认值: 500ms(中等速度)
  - 键盘调速步进: ±25ms(原±5ms，适配新范围)
  - SettingsWindow速度滑块映射同步更新到350-700区间

### 性能优化
- **图片缩放缓存**: DisplayWidget新增`_cached_scaled`缓存列表
  - paintEvent不再每帧调用`scaled()`(CPU密集操作)，改用预缓存图片
  - 窗口大小变化(resizeEvent)或图片更换(set_images)时自动重建缓存
- **UI更新节流**: `_tick()`中的进度显示优化
  - 进度条位置/百分比文字: 每帧更新(轻量操作，保持流畅)
  - 页码同步(`_sync_page_input`): 每3帧执行一次(原每帧O(n)遍历所有图片计算高度)
  - 提取`_sync_page_input()`独立方法，供_tick节流和update_progress_display全量更新共用

### 修改文件
- `guitar_tab_viewer.py` — DisplayWidget新增mousePressEvent/mouseMoveEvent/mouseReleaseEvent/_hit_annotation(拖动); _cached_scaled/_rebuild_image_cache/resizeEvent(图片缓存); _tick重构(内联轻量UI+_sync_page_input节流); speed_spin.setRange(350,700); show_display速度映射更新; 默认base_speed=500

---

## v1.4.0 (2026-06-07) - 标注导入导出 + 全局撤销重做 + A4导出 + 导航修复

### 新增功能
- **标注自动导入(同名文件)**: 打开谱子时自动加载同目录下同名 `.anno.json` 标注文件
  - 命名规则: `晚安北京.gp4` → `晚安北京.gp4.anno.json`（与谱子放一起，方便分发/备份）
  - 兼容旧路径: 优先加载新路径，找不到则回退到 `data/annotations/` 目录
- **标注实时保存**: 每次添加/修改/删除标注后自动写入 `.anno.json` 文件
- **A4导出(PNG+PDF，含标注渲染)**: 工具栏新增"导出"按钮，支持:
  - PNG格式: 高清位图(2480×3508px @300dpi)，适合查看/打印/分享
  - PDF格式: 矢量质量最佳，适合正式文档
  - 自动分页: 内容超过一页A4高度时自动分多页导出
  - 完整包含: 谱面图像 + 全部标注文字(颜色/字体/大小均保留)
- **全局统一撤销重做(Ctrl+Z/Y)**:
  - DisplayWindow内置全局撤销栈(`_undo_stack`)和重做栈(`_redo_stack`)
  - 画布双击添加标注、管理器内增删改操作、全部共享同一个撤销栈
  - 最大深度50步，超出自动丢弃最早记录
  - 快捷键: `Ctrl+Z` 撤销 / `Ctrl+Y` 重做（在DisplayWindow全局生效）

### Bug修复
- **资源管理器导航到文档文件夹**: `open_file_location()` 现在强制使用绝对路径(`os.path.abspath()`)，
  并改用`shell=True`调用explorer命令。相对路径会导致Windows资源管理器打开默认的"文档"文件夹而非目标位置。

### 技术原理
- 标注文件命名策略: 使用与源文件同名+`.anno.json`后缀，存放在谱子同目录
- 全局撤销栈设计: Command Pattern快照模式 — 每次变更前深拷贝当前annotations列表存入undo_stack
- A4导出流程: QPainter → QPixmap(QPNG) / QPdfWriter(PDF) → 绘制图片层 → 映射坐标绘制标注层 → 页码
- 导出坐标映射: 标注相对坐标(0-1) × 总内容高度 = 导出画布绝对像素位置

### 修改文件
- `guitar_tab_viewer.py` — 新增 `_export_to_a4()`/`_render_to_a4_png()`/`_render_to_a4_pdf()`/`_draw_annotations_on_canvas()`/`_get_a4_size_px()`; 重构 `_get_annotation_file_path()`/`_load_annotations()`/`_save_annotations()` 支持新旧双路径; 新增 `_anno_save_snapshot()`/`_anno_undo()`/`_anno_redo()` 全局撤销栈; AnnotationManagerDialog委托撤销到父窗口; keyPressEvent增加Ctrl+Z/Y; open_file_location修复绝对路径

---

## v1.3.2 (2026-06-07) - 播放光标(Playhead)

### 新增功能
- **播放光标(移动竖条)**: 播放时显示一条红色竖线，从左到右经过每个音符位置
  - 竖线自动跟随播放进度移动，经过哪个音就到哪个位置
  - 顶部带三角形指示器，竖线带发光效果
  - 仅在播放状态显示，暂停/停止时隐藏
- **当前小节高亮**: 播放到的那个小节自动高亮显示(淡蓝色背景+小节号标签)
- **时间线引擎**: `_build_playhead_timeline()` 预计算所有拍的音频时间和屏幕坐标，`_update_playhead()` 每帧通过二分查找定位

### 技术原理
- `TabRenderer.render()` 现在同时输出图像和布局数据(`last_layouts`)
- 布局数据: PageLayout → SystemLayout → MeasureLayout → BeatLayout (含x_center/x_start/x_end坐标)
- 每帧`_tick()`调用`_update_playhead()`从SynthEngine.current_time_ms映射到BeatLayout坐标
- DisplayWidget.`_draw_playhead()`将原始坐标按缩放比转换后绘制竖线和小节高亮

### 修改文件
- `gtp_engine/renderer/tab_renderer.py` — render()新增`last_layouts`属性存储布局数据
- `guitar_tab_viewer.py` — 新增 `_page_layouts`/`_playhead_info`/`_playhead_timeline` + `_build_playhead_timeline()`/`_update_playhead()`/DisplayWidget.`_draw_playhead()`

### Bug修复
- **scroll_step不响应speed_ms变化**: `_calculate_scroll_step()` 中 `total_ticks=1500` 为恒定值，导致 `speed_ms` 参数完全无效。速度滑块变化仅靠 `timer.start(val)` 改变定时器间隔来补偿(巧合生效)，但**速度曲线功能完全失效**(定时器固定在base_speed而scroll_step不变)。修复: 新公式 `scroll_step = distance × base_speed / (speed_ms × SCALE × 1000)` 使scroll_step与speed_ms成反比，速度曲线和滑块都能正确控制播放速度

---

## v1.3.1 (2026-06-07) - 音频多轨并轨 + 模式切换

### 新增功能
- **全轨并轨播放(默认)**: GTP文件打开后自动以"全轨并轨"模式播放音频，所有音轨同时发声（乐队合奏效果）
  - 每个音轨分配独立MIDI通道(0-15)，互不干扰
  - `MidiConverter.convert_all_tracks()` 合并所有轨道事件
- **3种音频模式切换**: 🔊按钮改为下拉菜单，支持:
  - `🔊 全轨并轨` - 所有音轨同时播放(默认)
  - `🎸 仅当前轨` - 只播放当前选中音轨
  - `🔇 关闭音频` - 纯滚动播放，不输出声音
- **自动初始化**: 打开GTP文件时音频引擎自动就绪(无需手动开启)

### 修改文件
- `gtp_engine/audio/midi_converter.py` — 新增 convert_all_tracks() / get_all_tracks_duration_ms() / **is_drum_track()** (鼓轨检测)
- `gtp_engine/audio/synth_engine.py` — 新增 **set_drum_kit()** (Bank MSB=128 鼓组设置)
- `guitar_tab_viewer.py` — 音频按钮→QToolButton下拉菜单 + closeEvent(关窗停音) + 进度条seek同步 + **通道9鼓组设置** + _set_audio_mode() + _rebuild_audio_events()双模式

---

## v1.3.0 (2026-06-07) - Phase 3 音频播放引擎

### 新增功能
- **MIDI事件序列转换器** (`gtp_engine/audio/midi_converter.py`):
- **FluidSynth 合成引擎** (`gtp_engine/audio/synth_engine.py`):
- **音频/视觉同步**:
- **优雅降级**: fluidsynth 未安装或 SoundFont 缺失时自动禁用音频按钮并提示

### 新增依赖
- `pyfluidsynth >= 1.4.0` (python-fluidsynth, 开源项目: nwhitehead/pyfluidsynth)
- `libfluidsynth-3.dll` (FluidSynth C运行时库, 放到项目根目录或 `fluidsnyth/bin/` 目录)

### Bug修复
- **关窗后音频继续播放**: 新增 `closeEvent()` 强制 stop + shutdown SynthEngine
- **进度条拖动不跳转音频**: `_on_progress_changed()` 增加 `synth_engine.seek()` 同步
- **并轨模式切轨停止播放**: `_on_gtp_track_changed()` 在"全轨并轨"模式下仅切换视觉显示，不停播/不重置位置/不重建事件
- **窗口大小影响播放速度**: `_calculate_scroll_step()` 从绝对像素(200/speed)改为相对距离(total_distance/1500ticks)，相同speed_ms下播放时长恒定，不受窗口尺寸影响
- **鼓轨发吉他声**: `MidiConverter` 新增 `is_drum_track()` 自动检测鼓轨(名称匹配+fret==pitch特征)，鼓轨映射到MIDI通道9；`SynthEngine` 新增 `set_drum_kit()` 显式设置 Bank MSB=128(标准鼓组)，不再设吉他音色
- **打击乐通道9警告**: 已修复——鼓轨正确使用通道9，非鼓轨自动跳过

### 修改文件
- `gtp_engine/__init__.py` — 导出 audio 模块(MidiConverter/SynthEngine)
- `gtp_engine/audio/midi_converter.py` — [新增] MIDI转换器
- `gtp_engine/audio/synth_engine.py` — [新增] FluidSynth合成引擎
- `gtp_engine/audio/__init__.py` — [新增] 包初始化
- `guitar_tab_viewer.py` — 集成音频播放到 DisplayWindow

## v1.2.3 (2026-06-07) - 技巧标签重构 + 击弦弧线 + 滑音s + 调弦简化

### 新增功能
- **击弦/勾弦弧线**: Hammer On / Pull Off 绘制向上凸圆弧连接两个同弦音符，弧线上方显示 H/P 文字
- **滑音s在斜杠上**: 上滑音显示 "s"、下滑音显示 "S"，文字位于斜杠中点
- **技巧标签移至弦线上方**: P.M./let ring/N.H./A.H./T.H./P.H. 全部使用统一虚线算法，画在每行谱子(y_tab_top)上方
- **泛音虚线支持**: N.H./A.H./T.H./P.H. 使用与P.M.相同的跨拍连线虚线算法（橙色 #F59E0B）
- **调弦简化显示**: 使用 `track.get_tuning_name()` 显示"标准调弦(EADGBE)"等，不再显示原始MIDI音高
- **第一页留白**: 第1页第一行系统下移15px，给标题区域更多呼吸空间

### Bug 修复
- **推弦不再显示"B"文字**: BEND已由_draw_bend_indicator图形化处理(弧线+度数文字)，SKIP_TECHS中排除避免重复
- **滑音/击弦不再重复文字**: SLIDE_UP/DOWN/HAMMER_ON/PULL_OFF 均从文字标签中排除

## v1.2.2 (2026-06-07) - 推弦GP5格式渲染 + 休止符规则优化

### Bug 修复
- **推弦箭头必须超出顶线**: 弧线峰值(箭头尖端)现在确保在六线谱最顶线(y_tab_top)之上至少4px。弧线高度动态计算：取基础高度和"起点到顶线外4px距离"中的较大值
- **□ 小方块问题**: 全休止符/二分休止符使用填充矩形 `drawRect` + `setBrush`，在 Solo 轨空小节中显示为白色小方块。改为 `Qt.NoBrush` 细线框样式
- **空小节休止符过多**: Intro 等空小节（无任何音符）不应显示任何休止符符号。新增规则1：检测整个小节是否完全为空（所有拍都是休止），是则完全不画
- **Solo段休止符杂乱**: 同上，空小节过滤后大幅改善

### 新增: GP5格式推弦渲染
推弦标记从简单弧线+文字"B"重写为严格参照 Guitar Pro 5 的标准格式:
- **度数文字**: 显示 "1/4"/"1/2"/"3/4"/"Full"（根据推弦量自动映射）
- **上弯弧线**: 从品格数字下方开始，向上弯曲到峰值，峰值处有向上箭头(▲)
- **释放段**: 有释放的推弦绘制下弯弧线回到原位，末端有向下箭头(▼)
- **动态高度**: 推弦量越大弧线越高（1/4=14px, 1/2=18px, Full=26px）

### 数据模型扩展
- **BendData 类** (`note.py`): 新增推弦详细数据模型，存储类型/度数/曲线点
- **解析器增强** (`gtp_parser.py`): 提取 GTP 库 BendEffect 的完整数据（type/value/points/maxValue）

### 新增: 动态弦数支持
弦线数量不再固定为6根，从GTP文件读取实际弦数:
- **SystemLayout 新增 `string_count` 字段**: 从 `track.string_count` 传入
- **布局引擎**: `tab_height = (string_count - 1) × TAB_LINE_SPACING`
- **渲染器**: `_draw_tab_lines()` 根据 `system.string_count` 绘制对应数量的弦线
- **信息栏**: TAB竖排文字、谱号竖线、拍号位置均根据实际弦数动态调整
- 支持: 4弦(贝斯)、5弦(5弦贝斯)、6弦(标准吉他)、7弦(7弦吉他)等

## 待规划功能
- [x] ~~GTP解析渲染~~ ✅ v1.1.0 | ~~音频播放~~ ✅ v1.3.0 | ~~技巧符号~~ ✅ v1.2.x | ~~多轨~~ ✅ v1.1.1
- [ ] 缩放 | 标注导入导出(已有基础) | 多语言 | 最近文件 | 全屏模式
