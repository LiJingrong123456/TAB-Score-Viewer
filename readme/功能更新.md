# 功能更新记录

## v2.0.1 (2026-06-13) - Bug修复

### Bug修复
- **GTP模式点击谱面前几行无法跳转进度**:
  - 原因1: `total_scroll_distance=0`时(短谱/一屏显示完)，整个点击处理被跳过，播放无法启动
  - 原因2: 居中偏移量过大(`height/2`)，点击前几行时`new_position`为负数→被`max(0,..)`截断为0→视觉位置不变
  - 原因3: 音频seek使用截断后的`new_position`，导致音频也无法跳到正确时间点（总是跳到开头）
  - 修复1: 将`toggle_playback()`提到条件外层，确保任何情况下点击都能启动/继续播放
  - 修复2: 偏移量从`height/2`减小到`height*0.25`(点击处显示在屏幕上1/4处,大幅减少顶部截断)
  - 修复3: 新增`seek_position`变量(原始计算值,不被clamp截断),音频seek使用此值确保跳到正确时间点

### 技术原理
- **偏移策略优化**: height/2居中 → height*0.25偏上(平衡"减少截断"与"保留上下文")
- **双位置分离**: 视觉位置(new_position)clamp防越界 + 音频位置(seek_position)保留原始精度
- 播放启动无条件执行（只要画布宽度有效且未在播放）

### 10条Case预期结果
| Case | 场景 | 预期结果 |
|------|------|----------|
| 1 | GTP长谱, position=300, 点击顶部(y=30) | new_pos≈130(非0!), seek到对应早前时间, 启动播放 |
| 2 | GTP长谱, position=0, 点击顶部 | new_pos=0, seek到开头, 启动播放 |
| 3 | GTP长谱, position=中间, 点击中部 | 正常跳转+seek+播放 |
| 4 | GTP长谱, position=中间, 点击底部 | 跳转到靠后位置+seek+播放 |
| 5 | GTP短谱(一屏显示完), 点击任意处 | total_dist=0不进入position块, 但仍启动播放 |
| 6 | 图片模式, 同样场景 | 行为一致(共用mousePressEvent) |
| 7 | 已播放状态, 点击谱面 | 继续播放(不重启toggle) |
| 8 | 点击标注区域 | 进入拖动模式(不受影响) |
| 9 | 播放中途点已滚过的前几行 | 视觉跳回+音频seek回对应时间 |
| 10 | 右键点击 | 不触发(不受影响) |

### 修改文件
- **修改** `TAB Score Viewer.py` — DisplayWidget.mousePressEvent() 偏移策略+双位置分离

---

## v2.0.0 (2026-06-13) - 深色/浅色主题系统 + UI美化

### 新功能
- **ThemeManager 主题管理器**: 全局深色/浅色主题统一管理
  - **单例+QObject**: 继承QObject，`pyqtSignal(str)` 作为类属性定义（符合PyQt5规范）
  - **动态切换**: 运行时实时切换深色/浅色主题，无需重启应用
  - **信号通知**: `theme_changed` 信号发射，DisplayWindow自动连接并刷新全部页面
  - **GTP渲染联动**: UI主题与ApolloTab渲染主题自动同步
    - 浅色UI → GTP谱使用 light 渲染主题
    - 深色UI → GTP谱使用 dark 渲染主题
    - 导出GTP谱 → 强制使用 light 浅色渲染（适合打印）

- **浅色主题配色方案(THEME_LIGHT)**: 精心设计的浅色模式配色
- **自定义组件主题适配**: ModernButton/ModernSlider/ProgressBarSlider 全部从 ThemeManager.get() 获取颜色
- **主题持久化**: settings.json 的 `theme` 字段，启动自动恢复
- **国际化支持**: theme_label / theme_tooltip 中英文翻译

### Bug修复
- **GTP模式暂停恢复播放**: 暂停后再播放从暂停位置继续，而非回到开始
  - 新增 `_paused_time_ms` 变量保存暂停时的播放时间
  - `stop_playback()` 新增 `reset_position` 参数区分暂停/停止
  - 播放按钮(暂停): 保存当前位置，恢复时继续
  - 停止按钮: 重置到开始位置
- **LoadContentWorker 方法缺失**: 新增 `_create_error_image()` 方法，修复加载失败时的错误显示
- **GTPPlayer.set_theme 兼容性**: 添加 `hasattr()` 检查，兼容不支持主题切换的旧版 GTPPlayer
- **ThemeManager pyqtSignal 规范修复**:
  - Issue1: 未继承 QObject → 改为 `class ThemeManager(QObject)` + `super().__init__()`
  - Issue2: 动态赋值实例属性 → `theme_changed = pyqtSignal(str)` 提升为类属性
  - 删除 `init_signal()` 延迟初始化方法（不再需要）
- **导出跨页溢出修复(v4)**:
  - PNG/PDF导出使用 `QPainter.setClipRect()` 绝对裁剪，彻底消除页底溢出
  - 边距从2%缩小到0.5%，图片间距从5px减到3px，最大化谱面显示区域
  - 预留30px页码区域，不与内容重叠
- **GTPPlayer.is_loaded 属性缺失**: ApolloTab/player.py 新增 `@property is_loaded`
- **导出强制浅色主题**: 当前轨(track_mode="current")导出路径也新建renderer+set_theme('light')
- **工具栏GTP主题按钮已移除**: GTP渲染主题完全跟随UI主题自动同步

### 技术原理
- **设计模式**: 单例模式(Singleton) + 观察者模式(Observer/pyqtSignal)
- **PyQt5信号规范**: pyqtSignal 必须是类属性 + 必须在QObject子类中定义
- **绝对裁剪**: setClipRect() 在渲染管线层面设置硬边界，不受浮点舍入影响

### 修改文件
- **重构** `TAB Score Viewer.py` — ThemeManager(QObject) + THEEM_LIGHT + _refresh_theme() + 导出v4防溢出
- **修改** `ApolloTab/player.py` — 新增 @property is_loaded + set_theme() 代理方法
- **更新** `locales/zh_CN.json`, `locales/en_US.json` — theme相关翻译

---

## v1.9.1 (2026-06-12) - 应用图标 + EXE打包支持

### 新功能
- **应用图标系统**: icon.ico 窗口图标 + 任务栏图标统一显示
- **PyInstaller 打包配置**: 新增 `guitar_tab_viewer.spec` onedir模式打包配置

### 修改文件
- **修改** `guitar_tab_viewer.py` — 新增 get_app_icon() 函数
- **新增** `guitar_tab_viewer.spec` — PyInstaller 配置
- **新增/更新** `icon.ico` — 多尺寸应用图标

---

## v1.9.0 (2026-06-12) - 多语言支持(i18n)

### 新功能
- **国际化系统(I18n)**: 完整的多语言支持框架(单例+JSON翻译+信号通知)
- **语言切换UI**: 设置窗口新增语言选择下拉框(中文/英文)
- **全面国际化覆盖**: 主窗口/设置窗口/对话框/进度栏等

### 修改文件
- **重构** `guitar_tab_viewer.py` — I18n类 + t()调用 + 语言切换UI
- **新增** `locales/zh_CN.json`, `locales/en_US.json`

---

## v1.8.2 (2026-06-12) - 库名更名 + 许可证更新

### 重构内容
- **库名更改**: `gtp_engine` → `ApolloTab`(目录/导入/配置/文档全量替换)
- **许可证**: MIT → MPL-2.0
- **Bug修复**: 语法错误/ImportError/AttributeError/播放条不显示等5个问题

---

## v1.8.1 (2026-06-12) - 主程序GTP代码库化完成

### 重构内容
- 完全解耦: 所有GTP代码迁移至ApolloTab库，统一通过GTPPlayer访问
- 接口统一: load/play/stop/seek/timeline/audio 全套API
- 代码清理: 移除约200行冗余内联代码

## 待规划功能
- [x] ~~GTP解析渲染~~ ✅ v1.1.0 | ~~音频播放~~ ✅ v1.3.0 | ~~技巧符号~~ ✅ v1.2.x | ~~多轨~~ ✅ v1.1.1 | ~~库化~~ ✅ v1.8.x | ~~多语言~~ ✅ v1.9.0 | ~~图标+打包~~ ✅ v1.9.1 | ~~主题系统~~ ✅ v2.0.0
- [ ] 缩放 | 标注导入导出(已有基础) | 最近文件 | 全屏模式
